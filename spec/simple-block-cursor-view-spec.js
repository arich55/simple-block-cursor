'use babel';

import SimpleBlockCursorView from '../lib/simple-block-cursor-view';

describe('SimpleBlockCursorView', () => {
  it('has one valid test', () => {
    expect('easy').toBe('easy');
  });
});
